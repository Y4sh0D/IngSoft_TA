<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="300dp"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="@drawable/popup_background"
    android:elevation="8dp">

    <EditText
        android:id="@+id/etNombreNuevoBarbero"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Nombre del barbero"
        android:inputType="textPersonName"
        android:drawableStart="@android:drawable/ic_menu_edit"
        android:drawablePadding="10dp"
        android:padding="12dp"
        android:background="@drawable/popup_background"
        android:textColor="@color/barber_black_deep"
        android:textColorHint="@color/barber_gray"
        android:fontFamily="@font/oswald_regular"
        android:layout_marginBottom="16dp" />

    <!-- Botón para seleccionar imagen -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnSeleccionarImagen"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:text="Seleccionar Imagen"
        android:textColor="@color/white"
        app:backgroundTint="@color/barber_blue_navy"
        app:cornerRadius="12dp"
        android:fontFamily="@font/oswald_bold"
        android:layout_marginBottom="12dp" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnCrearBarbero"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:text="Crear"
        android:textColor="@color/white"
        app:backgroundTint="@color/barber_black_deep"
        app:cornerRadius="12dp"
        android:fontFamily="@font/oswald_bold"
        android:layout_marginBottom="10dp" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnCancelar"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:text="Cancelar"
        android:textColor="@color/white"
        app:backgroundTint="@color/barber_gray"
        app:cornerRadius="12dp"
        android:fontFamily="@font/oswald_bold" />

</LinearLayout>
